name: Release Build and Publish

on:
  push:
    branches:
      - 'release/**'
      - 'release/*'
      - 'release-*'
      - 'releases/**'
      - 'releases/*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build, Test, and Release
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Determine Version
        id: version_step
        shell: pwsh
        run: |
          $branch = "${{ github.ref_name }}"
          $ver = $branch -replace '^releases?/', '' -replace '^releases?-', ''
          if (-not $ver -or $ver -eq $branch) {
            $ver = "v${{ github.run_number }}"
          } elseif (-not ($ver.StartsWith("v") -or $ver.StartsWith("V"))) {
            $ver = "v$ver"
          }
          Write-Host "Calculated version: $ver"
          echo "RELEASE_VERSION=$ver" >> $env:GITHUB_ENV

      - name: Restore dependencies
        run: dotnet restore FlightPlan.sln

      - name: Build Solution
        run: dotnet build FlightPlan.sln -c Release --no-restore

      - name: Run Tests
        run: dotnet test FlightPlan.sln -c Release --no-build --verbosity normal

      - name: Publish App (win-x64 self-contained)
        run: |
          dotnet publish FlightPlan.csproj -c Release -r win-x64 --self-contained true -o ./publish/win-x64

      - name: Publish Single-File Executable (win-x64 self-contained)
        run: |
          dotnet publish FlightPlan.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -o ./publish/single-file

      - name: Package Release Assets
        shell: pwsh
        run: |
          $zipName = "FlightPlan-${{ env.RELEASE_VERSION }}-win-x64.zip"
          Compress-Archive -Path ./publish/win-x64/* -DestinationPath "./$zipName"
          Write-Host "Created $zipName"

          $singleExeName = "FlightPlan-${{ env.RELEASE_VERSION }}-win-x64.exe"
          Copy-Item -Path ./publish/single-file/FlightPlan.exe -Destination "./$singleExeName"
          Write-Host "Created $singleExeName"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_VERSION }}
          name: Release ${{ env.RELEASE_VERSION }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            FlightPlan-*-win-x64.zip
            FlightPlan-*-win-x64.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
