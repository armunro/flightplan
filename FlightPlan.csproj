<Project Sdk="Microsoft.NET.Sdk.Web">

    <PropertyGroup>
        <TargetFramework>net9.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <ApplicationIcon>wwwroot\favicon.ico</ApplicationIcon>
        <MSBuildEnableWorkloadResolver>false</MSBuildEnableWorkloadResolver>
        <UsingMicrosoftNETSdkWorkloads>false</UsingMicrosoftNETSdkWorkloads>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Atlassian.SDK" Version="13.0.0" />
      <PackageReference Include="Octokit" Version="14.0.0" />
      <PackageReference Include="Autofac.Extensions.DependencyInjection" Version="11.0.0" />
      <PackageReference Include="Quartz.AspNetCore" Version="3.18.2" />
      <PackageReference Include="YamlDotNet" Version="17.1.0" />
      <PackageReference Include="Microsoft.Graph" Version="5.62.0" />
      <PackageReference Include="Azure.Identity" Version="1.13.1" />
    </ItemGroup>

    <ItemGroup>
      <Content Remove="wwwroot\assets\email.html" />
      <Content Update="frontend\**" CopyToOutputDirectory="Never" />
    </ItemGroup>

 


    <Target Name="BuildFrontend" BeforeTargets="BeforeBuild">
        <Message Importance="high" Text="--- Building Frontend ---" />
        <Exec WorkingDirectory="frontend" Command="npm install" />
        <Exec WorkingDirectory="frontend" Command="npm run build" />
    </Target>

    <Target Name="CopyFrontendToOutput" AfterTargets="BuildFrontend" BeforeTargets="Build">
        <ItemGroup>
            <FrontendFiles Include="wwwroot\**" />
        </ItemGroup>
        <Copy SourceFiles="@(FrontendFiles)" DestinationFolder="$(OutputPath)wwwroot\%(RecursiveDir)" SkipUnchangedFiles="true" />
    </Target>

</Project>
